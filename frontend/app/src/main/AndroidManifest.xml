<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>


    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:name="pl.put.swWrotaDlaKota.SwWrotaDlaKotaApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_loading"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_loading"
        android:supportsRtl="true"
        android:theme="@style/Theme.swWrotaDlaKota"
        tools:targetApi="31">
        <activity
            android:name="pl.put.swWrotaDlaKota.ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.swWrotaDlaKota">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>